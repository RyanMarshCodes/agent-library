## Session — 2026-03-27 | Cursor | ai-docs/agents naming

### What was worked on
- Standardized all agent definition filenames under `ai-docs/agents/` to `kebab-case.agent.md`.
- Removed duplicate `TechDebtAnalysisAgent.md` (content aligned with `tech-debt-analysis.agent.md`).
- Dropped legacy `code-simplifier.md` and Angular-specific `security-check.md` in favor of stack-agnostic `code-simplifier.agent.md` and `security-check.agent.md` (from former `CodeSimplifierAgent.md` / `SecurityCheckAgent.md`).

### Decisions made
- **File naming**: `*.agent.md` everywhere under `ai-docs/agents/`, matching MCP ingestion patterns (`AgentIngestionCoordinator` uses `*.agent.md`).
- **Catalog**: `name` stays PascalCase for stable IDs; `file` points at the new kebab paths; `namingConvention` field updated; version bumped to `0.4.0`.

### Issues encountered
- Root-wide `grep` hit a Windows `nul` path error; used scoped searches under `ai-docs` / `.claude` / `mcp-server` instead.

### Files changed
- `ai-docs/agents/*` — renames (see directory listing).
- `ai-docs/catalog.json`
- `ai-docs/README.md`
- `ai-docs/MIGRATION-CHECKLIST.md`
- `ai-docs/skills-plan.md`
- `.claude/skills/*/SKILL.md` — updated agent paths.
- `mcp-server/src/Ryan.MCP.Mcp/Services/AgentEntry.cs` — doc comment example filename.
- `ai-docs/agents/ai-agent-expert.agent.md` — catalog path fix (`ai-docs/catalog.json`).

### Open items / follow-ups
- If anything external (docs, other repos, or bookmarks) still referenced old filenames, update those references on discovery.

---

## Session — 2026-03-27 | Cursor | Ryan.MCP external MCP bridge

### What was worked on
- Exposed downstream MCP tools (including Aspire `memory` on port 8801) on the main Ryan.MCP surface via generic bridge tools.

### Decisions made
- **Generic tools** (`list_external_mcp_tools`, `call_external_mcp_tool`) instead of duplicating every memory tool name on the host; any enabled `ExternalConnectors` entry can be called the same way.
- **`ExternalConnectorRegistry.TryGetEnabled`** for case-insensitive connector lookup.
- **Header placeholders** `${env:VAR}` expanded when opening HTTP MCP client sessions (matches existing appsettings pattern for GitHub/ADO).

### Files changed
- `mcp-server/src/Ryan.MCP.Mcp/Services/ExternalMcpClientService.cs` (new)
- `mcp-server/src/Ryan.MCP.Mcp/Services/ExternalConnectorRegistry.cs`
- `mcp-server/src/Ryan.MCP.Mcp/McpTools/ConnectorTools.cs`
- `mcp-server/src/Ryan.MCP.Mcp/Program.cs`

### Open items / follow-ups
- No automated tests yet for `ExternalMcpClientService` (no test project under `mcp-server/`); consider integration test against a mock HTTP MCP or containerized memory when CI allows.
