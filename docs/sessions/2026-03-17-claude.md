## Session — 2026-03-17 | Claude (claude-sonnet-4-6) | agent-configurations setup

### What was worked on
- Reviewed the existing project structure: `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, tool-specific entry points, `backend/csharp/`, `global/`
- Established the intended folder layout: `backend/` for language-specific, `frontend/` for framework-specific, `global/` for language/framework-agnostic cross-cutting standards
- Set up `ai-docs/` as a long-term memory system for AI agents

### Decisions made
- `ai-docs/` is append-only historical record — agents read it at session start, write at session end
- Session files named `YYYY-MM-DD-{agent}.md`; multiple sessions same day append with `---` separator
- `issues-resolutions.md` is newest-first, append-only; never delete historical entries
- `global/` holds cross-cutting, language-agnostic standards (not language/framework-specific)
- `frontend/` folder does not exist yet — will be created when frontend conventions are added

### Issues encountered
- `global/unit-testing.md` claims to be language-agnostic but contains C#-only code examples. Flagged to user; not yet resolved. Consider either making examples pseudo-code/multi-language or moving C# examples to `backend/csharp/`.

### Files changed
- `README.md` — updated structure diagram to include `frontend/`, all entry-point files, and `ai-docs/`; updated Quick Reference and Usage/Contributing sections
- `AGENTS.md` — added `## Long-term memory (ai-docs/)` section with session start/end instructions; updated Code Style section to reference `backend/csharp/` and `frontend/` paths
- `ai-docs/README.md` — full rewrite: now defines the memory system structure, session file format, reading priority, and what belongs/doesn't belong
- `ai-docs/issues-resolutions.md` — created; append-only issues log
- `ai-docs/sessions/2026-03-17-claude.md` — created (this file)

### Open items / follow-ups
- Reconcile `global/unit-testing.md` C#-specific examples (make language-agnostic or split)
- Create `frontend/` folder with initial framework conventions (React, Angular, or TypeScript)
- Consider adding more files to `global/` (e.g. `api-design.md`, `git-conventions.md`)
